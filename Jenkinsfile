currentBuild.displayName = "first-#"+currentBuild.number

pipeline{
    agent any
    stages{
        stage("Getlost"){
        steps{
            echo "Yahase nikalo"
             }
        }
    }
}
